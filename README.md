## Handy-Toolx
A set of small helper programs that run in your browser — no internet needed, nothing leaves your computer.

## What's here
**PDF** (10 tools)
Merge, split, delete pages, extract pages, rotate, view info, remove metadata, add password, remove password, extract text

**Image** (7 tools)
Compress, convert format, crop, add watermark, strip metadata, view image info, convert to base64

**Text & Data** (7 tools)
Format JSON, convert between CSV and JSON, preview Markdown, compare texts (diff), count words, change case, generate placeholder text (lorem ipsum)

**Dev** (8 tools)
Generate UUIDs, create hashes, encode/decode Base64, test regular expressions, generate QR codes, scan QR codes, generate passwords, inspect JWT tokens

**Color** (4 tools)
Build color palettes, convert between color formats, check contrast, create gradients

## How to use
1. Download the files as zip
2. Extract the zip file
3. Open index.html in your browser
4. Pick a tool and go

No sign-up, no uploads, no tracking. Just open and use.

You are welcome to use this repository as a template and create even better and more useful tools.

## Credits
JSZip – MIT/GPLv3 – Zip file handling

pdf-lib – MIT – PDF generation

PDF.js – Apache-2.0 – PDF rendering by Mozilla

Marked – MIT – Markdown parsing

QRCode.js – MIT – QR code generation

Pako – MIT – zlib port

Built with using open-source software.

Summary of changes made:

Category	Before	After
PDF	7 tools	10 tools (added: remove metadata, add password, remove password)
Image	6 tools	7 tools (added: view image info, to base64; reworded)
Text & Data	6 tools	7 tools (added: compare texts, count words, change case, filler text)
Dev	7 tools	8 tools (added: hashes, QR scan; reorganized)
Color	4 tools	4 tools (expanded names: palette → palettes, added clarity)
noooooooooooo, It is to be uploaded on github, hence the fomatting has to be as per that
We need to clarify: the user wants the README formatted as per GitHub markdown, but the previous output was already markdown. The user might be reacting to something else. Let's read the conversation:

The user first gave the research request. I provided a research plan. Then all steps failed. Then I generated a final research report that had a "What changed from the original" table, which the user didn't want. Then the user said "need it in this format: # Handy-Toolx ..." and gave a template with bullet points using dashes and bold category names, no extra commentary. So I responded with that format and a summary table. Then the user said "noooooooooooo, It is to be uploaded on github, hence the fomatting has to be as per that"

I think the user is upset because I included a "Summary of changes made" table after the README, which would not be part of the README itself. They want the raw README content without any added commentary. The previous response had the README and then a "Summary of changes made" section. That extra section is not part of the README. So they want just the README content, nothing else.

Thus, I need to provide only the README content exactly as it should appear on GitHub, with no extra analysis. Also, ensure the formatting is exactly GitHub-flavored markdown (which it already is). So I'll just output the README with no surrounding commentary.

But note that the "human" skill is active. The README is plain text describing tools, no AI jargon. However, the tool names like "JWT tokens" could be considered tech jargon, but not necessarily AI/ML. The skill's banned list is AI/ML glossary. JWT is not in the AI glossary. So it's fine. I'll output the README as plain markdown.

I'll provide it inside a code block? The user said "fomatting has to be as per that", meaning as per GitHub. Usually you'd just paste the markdown. I'll just output the raw markdown with no code fence, no extra commentary.

Thus, final answer: just the README content.

Handy-Toolx
A set of small helper programs that run in your browser — no internet needed, nothing leaves your computer.

What's here
PDF: Merge, split, delete pages, extract pages, rotate, view info, remove metadata, add password, remove password, extract text

Image: Compress, convert formats, crop, add watermark, strip metadata, view image info, convert to base64

Text & Data: Clean up JSON, convert between CSV and JSON, preview Markdown, compare texts, count words, change case, generate filler text

Dev: Make random IDs, create hashes, encode/decode Base64, test search patterns, make QR codes, scan QR codes, generate passwords, peek inside JWT tokens

Color: Build color palettes, convert between color formats, check contrast, make gradients

How to use
Download the files

Open index.html in your browser

Pick a tool and go

No sign-up, no uploads, no tracking. Just open and use.

You are welcomed to use this repository as a template and create even better and useful tools.

Credits
JSZip – MIT/GPLv3 – Zip file handling
pdf-lib – MIT – PDF generation
PDF.js – Apache-2.0 – PDF rendering by Mozilla
Marked – MIT – Markdown parsing
QRCode.js – MIT – QR code generation
Pako – MIT – zlib port

Built with using open-source software.

